server:
  port: 9004

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nmvvskb/config-server/
          default-label: main
          clone-on-start: true
